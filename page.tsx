'use client'
import {useState} from 'react'
export default function Editor(){const [name,setName]=useState('عبدالرحمن السويطي');const [job,setJob]=useState('إدارة مكتبية');const [email,setEmail]=useState('aytb9648@gmail.com');const [phone,setPhone]=useState('0544873310');const [skills,setSkills]=useState('التواصل، العمل الجماعي، إدارة الوقت، حل المشكلات');const [color,setColor]=useState('#0f6b4f');function save(){localStorage.setItem('rawaj-cv',JSON.stringify({name,job,email,phone,skills,color}));alert('تم حفظ السيرة الذاتية')}function download(){window.print()}return <main className="container"><h1>محرر السيرة الذاتية</h1><div className="editor"><div className="card"><label>الاسم</label><input className="input" value={name} onChange={e=>setName(e.target.value)}/><label>المسمى الوظيفي</label><input className="input" value={job} onChange={e=>setJob(e.target.value)}/><label>البريد</label><input className="input" value={email} onChange={e=>setEmail(e.target.value)}/><label>الجوال</label><input className="input" value={phone} onChange={e=>setPhone(e.target.value)}/><label>المهارات</label><textarea value={skills} onChange={e=>setSkills(e.target.value)}/><label>لون القالب</label><input className="input" type="color" value={color} onChange={e=>setColor(e.target.value)}/><div className="actions"><button className="btn" onClick={save}>حفظ</button><button className="btn gold" onClick={download}>تحميل PDF</button></div></div><div className="cv" style={{borderTopColor:color}}><h1 style={{color}}>{name}</h1><p>{job}</p><p>{email} | {phone}</p><hr/><h3>الملخص</h3><p>محترف طموح يمتلك مهارات عالية في التنظيم وخدمة العملاء والعمل ضمن فريق.</p><h3>المهارات</h3><p>{skills}</p><h3>الخبرات</h3><p>شركة السيف مهندسون مقاولون المحدودة - مندوب توصيل</p><h3>المؤهل</h3><p>دبلوم إدارة مكتبية</p></div></div></main>}
